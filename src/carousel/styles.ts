/**
 * Swiper CSS imports — import this file in your app entry point
 * or let the Carousel component auto-import them.
 *
 * To avoid global CSS side-effects, import this explicitly
 * in your app instead of letting the component do it:
 *
 *   import '@mdigital_ui/ui/carousel/styles'
 */
import 'swiper/css'
import 'swiper/css/pagination'
